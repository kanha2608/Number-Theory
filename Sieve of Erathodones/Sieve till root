Instead of outer loop going till n 
we can reduce it to go to sqrt(n) because we know all greater prime will be true and that wouldn't contribute to their factor.

int n;
vector<bool> is_prime(n+1, true);
is_prime[0] = is_prime[1] = false;
for (int i = 2; i * i <= n; i++) {
    if (is_prime[i]) {
        for (int j = i * i; j <= n; j += i)
            is_prime[j] = false;
    }
}
